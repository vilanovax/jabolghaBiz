# سند طراحی بازی (Game Design Document)
# جابلقابیز — JabolghaBiz

---

## فهرست مطالب

1. [عنوان و معرفی بازی](#1-عنوان-و-معرفی-بازی)
2. [استک فنی](#2-استک-فنی)
3. [ساختار فایل‌ها](#3-ساختار-فایلها)
4. [سیستم‌های اصلی بازی](#4-سیستمهای-اصلی-بازی)
5. [فرمول‌ها و محاسبات](#5-فرمولها-و-محاسبات)
6. [قالب‌های کسب‌وکار](#6-قالبهای-کسبوکار)
7. [صفحات و رابط کاربری](#7-صفحات-و-رابط-کاربری)
8. [سیستم ناوبری](#8-سیستم-ناوبری)
9. [طراحی بصری](#9-طراحی-بصری)
10. [برنامه‌های آینده](#10-برنامههای-آینده)

---

## 1. عنوان و معرفی بازی

| فیلد | مقدار |
|------|-------|
| **نام بازی** | جابلقابیز (JabolghaBiz) |
| **نسخه** | 0.2.0 |
| **عنوان کامل** | جابلقابیز — امپراتوری کسب‌وکار |
| **شعار** | از هیچ شروع کن، کسب‌وکار بساز، تجارت کن، رقابت کن و بازار رو تصاحب کن. |
| **ژانر** | Idle Tycoon / Business Simulation |
| **پلتفرم** | وب (Mobile-first PWA) |
| **زبان** | فارسی (RTL) |
| **مخاطب هدف** | بازیکنان فارسی‌زبان علاقه‌مند به بازی‌های مدیریتی و idle |

### خلاصه بازی

بازیکن با یک سرمایه اولیه شروع کرده، کسب‌وکارهای مختلف (استارتاپ، مزرعه، رستوران، کارخانه، سوپرمارکت، حمل‌ونقل) می‌سازد. هر کسب‌وکار بر اساس سیکل‌های زمانی (timer-based idle mechanic) درآمد تولید می‌کند. بازیکن با استخدام کارمندان، آنلاک محصولات، ارتقا سطح و تجارت در بازار، امپراتوری خود را گسترش می‌دهد.

---

## 2. استک فنی

| تکنولوژی | نسخه | توضیح |
|-----------|-------|-------|
| **Next.js** | `16.1.6` | فریمورک React با App Router |
| **React** | `19.2.3` | کتابخانه UI |
| **React DOM** | `19.2.3` | رندر DOM |
| **Zustand** | `^5.0.11` | مدیریت State (بدون Redux) |
| **Tailwind CSS** | `^4` | استایل‌دهی utility-first |
| **@tailwindcss/postcss** | `^4` | پلاگین PostCSS |
| **Lucide React** | `^0.577.0` | آیکون‌ها (SVG icons) |
| **Vazirmatn** | `^33.0.3` | فونت فارسی |
| **TypeScript** | `^5` | تایپ‌گذاری استاتیک |
| **ESLint** | `^9` | لینتر کد |
| **eslint-config-next** | `16.1.6` | قوانین ESLint مخصوص Next.js |

---

## 3. ساختار فایل‌ها

```
jabolgha-biz/
├── package.json
├── src/
│   ├── types/
│   │   └── index.ts                    # تمام مدل‌های داده (interfaces & types)
│   ├── data/
│   │   └── mock.ts                     # داده‌های اولیه و قالب‌های کسب‌وکار
│   ├── store/
│   │   └── gameStore.ts                # Zustand store — تمام منطق بازی
│   ├── app/
│   │   ├── layout.tsx                  # لایه اصلی (RTL, فونت, GameShell)
│   │   ├── globals.css                 # استایل‌های سراسری
│   │   ├── page.tsx                    # صفحه اصلی (داشبورد)
│   │   ├── business/
│   │   │   ├── page.tsx                # لیست کسب‌وکارها
│   │   │   └── [id]/
│   │   │       └── page.tsx            # جزئیات یک کسب‌وکار
│   │   ├── market/
│   │   │   └── page.tsx                # بازار (قیمت‌ها + آگهی‌ها)
│   │   ├── leaderboard/
│   │   │   └── page.tsx                # رتبه‌بندی
│   │   ├── profile/
│   │   │   └── page.tsx                # پروفایل + بازار جمعه
│   │   └── settings/
│   │       └── page.tsx                # تنظیمات
│   └── components/
│       ├── layout/
│       │   ├── GameShell.tsx            # Shell کلی (TopBar + Main + BottomNav + useGameTick)
│       │   ├── TopBar.tsx              # نوار بالای صفحه
│       │   └── BottomNav.tsx           # ناوبری پایین صفحه
│       ├── ui/
│       │   ├── Card.tsx                # کارت با glow اختیاری
│       │   ├── Button.tsx              # دکمه با 4 واریانت و 3 سایز
│       │   ├── MoneyDisplay.tsx        # نمایش پول با فرمت فارسی
│       │   ├── Badge.tsx               # بج رنگی
│       │   └── StatBar.tsx             # نوار پیشرفت آمار
│       ├── business/
│       │   ├── BusinessCard.tsx         # کارت کسب‌وکار با تایمر
│       │   └── NewBusinessModal.tsx     # مودال ساخت کسب‌وکار جدید
│       ├── market/
│       │   ├── ProductPriceCard.tsx     # کارت قیمت محصول با نمودار مینی
│       │   └── MarketListingCard.tsx    # کارت آگهی بازار
│       ├── social/
│       │   └── LeaderboardRow.tsx       # ردیف رتبه‌بندی
│       └── player/
│           └── PlayerStatsPanel.tsx     # پنل آمار شخصی بازیکن
```

---

## 4. سیستم‌های اصلی بازی

### 4.1 سیستم بازیکن (Player System)

بازیکن (`PlayerProfile`) شامل اطلاعات زیر است:

| فیلد | نوع | توضیح | مقدار اولیه (Mock) |
|------|------|-------|-------------------|
| `id` | `string` | شناسه یکتا | `player-1` |
| `avatar` | `string` | اموجی آواتار | `👤` |
| `username` | `string` | نام کاربری | `تاجرباشی` |
| `level` | `number` | سطح بازیکن | `5` |
| `reputation` | `number` | اعتبار | `72` |
| `balance` | `number` | موجودی (تومان) | `150,000` |
| `stats` | `PlayerStats` | وضعیت شخصی | (جدول زیر) |
| `createdAt` | `string` | تاریخ عضویت | `1404-10-25` |

#### وضعیت شخصی (PlayerStats)

تمام مقادیر بین **0 تا 100** هستند و با `Math.max(0, Math.min(100, ...))` محدود (clamp) می‌شوند.

| ویژگی | کلید | آیکون | رنگ | مقدار اولیه |
|--------|------|-------|-----|-------------|
| شادی | `happiness` | 😊 | `#f472b6` (صورتی) | 75 |
| گرسنگی | `hunger` | 🍔 | `#fb923c` (نارنجی) | 60 |
| انرژی | `energy` | ⚡ | `#facc15` (زرد) | 80 |
| هوش | `intelligence` | 🧠 | `#818cf8` (بنفش) | 65 |
| تجربه | `experience` | ⭐ | `#34d399` (سبز) | 45 |

#### ارزش امپراتوری (Empire Value)

```
companyValue = baseRevenue * level * 10
empireValue = balance + SUM(companyValue) for all businesses
```

---

### 4.2 سیستم کسب‌وکار (Business System)

هسته اصلی بازی، مکانیک **Idle/Tycoon Timer** است:

#### چرخه زندگی کسب‌وکار

1. **ساخت**: بازیکن یک قالب انتخاب و هزینه آغاز (`startCost`) را پرداخت می‌کند
2. **تولید**: هر `cycleDuration` ثانیه، یک سیکل کامل می‌شود و درآمد خالص تولید می‌شود
3. **انباشت**: درآمد تولیدشده به `pendingRevenue` اضافه می‌شود (حداکثر `maxPendingCycles` سیکل)
4. **جمع‌آوری**: بازیکن دستی جمع‌آوری می‌کند **یا** اگر حسابدار استخدام شده، خودکار انجام می‌شود
5. **ارتقا**: با پرداخت `upgradeCost`، سطح بالا رفته و درآمد پایه 25% افزایش می‌یابد

#### فیلدهای کسب‌وکار (Business Interface)

| فیلد | نوع | توضیح |
|------|------|-------|
| `id` | `string` | شناسه یکتا |
| `ownerId` | `string` | مالک |
| `name` | `string` | نام (قابل سفارشی‌سازی) |
| `type` | `BusinessType` | نوع (6 نوع مختلف) |
| `level` | `number` | سطح فعلی |
| `icon` | `string` | اموجی |
| `baseRevenue` | `number` | درآمد پایه هر سیکل |
| `cycleDuration` | `number` | مدت هر سیکل (ثانیه) |
| `lastCycleAt` | `number` | timestamp آخرین سیکل |
| `pendingRevenue` | `number` | درآمد جمع‌نشده |
| `maxPendingCycles` | `number` | حداکثر سیکل‌های انباشته (50) |
| `expenses` | `number` | هزینه پایه هر سیکل |
| `upgradeCost` | `number` | هزینه ارتقا |
| `employees` | `HiredEmployee[]` | کارمندان استخدام‌شده |
| `products` | `BusinessProduct[]` | محصولات (قفل/آنلاک) |
| `initialEquipment` | `string` | تجهیزات اولیه |

#### مکانیک تایمر (Tick System)

از **یک تایمر سراسری واحد** استفاده می‌شود. هوک `useGameTick` در `GameShell` مستقر است و تابع `tickBusinesses()` را هر **1 ثانیه** فراخوانی می‌کند. هیچ کامپوننت دیگری `setInterval` مخصوص tick ندارد — کامپوننت‌ها فقط محاسبه read-only پیشرفت انجام می‌دهند.

```
elapsed = (now - lastCycleAt) / 1000
completedCycles = floor(elapsed / cycleDuration)

if completedCycles > 0:
    effectiveRevenue = calcEffectiveRevenue(biz)
    totalExpenses = calcTotalExpenses(biz)
    netPerCycle = max(0, effectiveRevenue - totalExpenses)
    cappedCycles = min(completedCycles, maxPendingCycles)
    newPending = pendingRevenue + netPerCycle * cappedCycles
    maxPending = netPerCycle * maxPendingCycles
    actualPending = min(newPending, maxPending)

    if hasAccountant:
        balance += actualPending        // جمع‌آوری خودکار
        pendingRevenue = 0
    else:
        pendingRevenue = actualPending  // انتظار جمع‌آوری دستی

    lastCycleAt += completedCycles * cycleDuration * 1000
```

> **نکته**: مقدار `maxPendingCycles` برای تمام کسب‌وکارها **50** است (قبلا بین 3 تا 5 بود).

#### نوار پیشرفت (Progress Bar)

```
remaining = max(0, cycleDuration - (elapsed % cycleDuration))
progress = ((cycleDuration - remaining) / cycleDuration) * 100
```

در صفحه جزئیات کسب‌وکار، یک **حلقه پیشرفت SVG** (progress ring) نمایش داده می‌شود:
```
strokeDasharray = "${progress * 2.64} 264"    // محیط دایره با r=42: ~264
```

---

### 4.3 سیستم کارمندان (Employee System)

#### نقش‌ها (Roles)

| نقش | کلید | توضیح | ویژگی اصلی |
|------|------|-------|------------|
| نیروی پایه | `base` | برنامه‌نویس، کشاورز، آشپز | افزایش درآمد پایه (revenueBoost) |
| مدیر | `manager` | مدیر پروژه، مدیر مزرعه، مدیر رستوران | بالاترین ضریب افزایش درآمد |
| حسابدار | `accountant` | حسابدار شرکت | **جمع‌آوری اتوماتیک درآمد** (`autoCollect: true`) |
| بازاریاب | `marketer` | بازاریاب دیجیتال | افزایش درآمد محصولات |
| فروش | `sales` | کارشناس فروش، گارسون | افزایش سرعت فروش |

#### سقف ضریب نیروها (Staff Multiplier Cap)

مجموع بوست درآمد تمام کارمندان به صورت ضریب (`staffMultiplier`) محاسبه می‌شود و **حداکثر 2.5x** (یعنی حداکثر 150% افزایش) است. حتی اگر هر 5 کارمند استخدام شده باشند، ضریب کل از 2.5 بیشتر نمی‌شود.

```
staffBoostSum = SUM(emp.revenueBoost) for all employees
staffMultiplier = min(1 + staffBoostSum, 2.5)  // max 150% boost
```

#### قوانین استخدام

- هر کارمند فقط **یک بار** قابل استخدام است (بر اساس `templateId`)
- پرداخت `hireCost` از موجودی بازیکن
- پس از استخدام، `salary` هر سیکل از هزینه‌ها کسر می‌شود
- `revenueBoost` به صورت ضریب از `baseRevenue` اعمال می‌شود (با سقف 2.5x)

#### تاثیر حسابدار

وجود حسابدار (`autoCollect: true`) باعث می‌شود که در هر tick، `pendingRevenue` خودکار به `balance` اضافه شود بدون نیاز به جمع‌آوری دستی.

---

### 4.4 سیستم محصولات (Product System)

هر کسب‌وکار مجموعه‌ای از **محصولات قابل آنلاک** دارد:

| فیلد | توضیح |
|------|-------|
| `name` | نام محصول |
| `icon` | اموجی |
| `description` | توضیح |
| `unlockCost` | هزینه آنلاک |
| `revenueBoost` | **افزایش مطلق درآمد** هر سیکل (تومان) |
| `unlocked` | وضعیت قفل/آنلاک |

#### نکته مهم
`revenueBoost` محصولات **افزایش مطلق** است (نه درصدی)، برخلاف کارمندان که **ضریب درصدی** دارند.

---

### 4.5 سیستم ارتقا (Upgrade System)

با ارتقا:
- `level` یک واحد افزایش می‌یابد (حداکثر `maxLevel = 20`)
- `baseRevenue` **25% افزایش** می‌یابد: `Math.round(baseRevenue * 1.25)`
- `upgradeCost` **60% گران‌تر** می‌شود: `Math.round(upgradeCost * 1.6)`
- ظرفیت نیرو و محصولات ممکن است افزایش یابد (بخش 4.10)

#### هزینه اولیه ارتقا (هنگام ساخت کسب‌وکار)

```
initialUpgradeCost = Math.round(startCost * 1.5)
```

---

### 4.6 سیستم بازار (Market System)

بازار دو بخش دارد:

#### 4.6.1 قیمت‌ها (Prices)

هر محصول (`Product`) شامل:

| فیلد | توضیح |
|------|-------|
| `name` | نام محصول |
| `category` | دسته‌بندی: `raw_material`, `processed`, `finished_good`, `food`, `tech`, `service` |
| `basePrice` | قیمت پایه |
| `currentPrice` | قیمت فعلی |
| `supply` | عرضه |
| `demand` | تقاضا |
| `priceHistory` | آرایه 7 عنصری تاریخچه قیمت |

**برچسب تقاضا:**
```
ratio = demand / supply
if ratio > 1.3 → "بالا" (سبز)
if ratio > 0.8 → "متوسط" (زرد)
else           → "پایین" (قرمز)
```

**برچسب عرضه:**
```
ratio = supply / demand
if ratio > 1.3 → "بالا" (سبز)
if ratio > 0.8 → "متوسط" (زرد)
else           → "پایین" (قرمز)
```

**نمودار مینی**: نمودار میله‌ای از `priceHistory` (7 میله) — آخرین میله سبز (افزایش) یا قرمز (کاهش).

#### 4.6.2 آگهی‌ها (Listings)

بازیکنان می‌توانند محصولات را بخرند:

| فیلد | توضیح |
|------|-------|
| `sellerId` | شناسه فروشنده |
| `sellerName` | نام فروشنده |
| `productName` | نام محصول |
| `quantity` | تعداد |
| `pricePerUnit` | قیمت هر واحد |

**قوانین خرید:**
- نمی‌توان آگهی خود را خرید (نمایش "آگهی شما")
- خرید 1 واحد در هر کلیک
- کسر `pricePerUnit * quantity` از موجودی
- اگر `quantity` به 0 برسد، آگهی حذف می‌شود

#### محصولات بازار (Mock Data)

| محصول | دسته | قیمت پایه | قیمت فعلی | عرضه | تقاضا |
|-------|------|-----------|-----------|------|-------|
| گندم 🌾 | ماده خام | 10 | 12 | 500 | 450 |
| آرد 🫘 | فرآوری‌شده | 25 | 28 | 300 | 350 |
| نان 🍞 | کالای نهایی | 50 | 55 | 200 | 280 |
| گوجه 🍅 | ماده خام | 8 | 7 | 600 | 400 |
| شیر 🥛 | ماده خام | 15 | 18 | 250 | 320 |
| پنیر 🧀 | فرآوری‌شده | 40 | 45 | 150 | 200 |
| پیتزا 🍕 | غذا | 80 | 85 | 100 | 180 |
| اپلیکیشن موبایل 📱 | فناوری | 200 | 220 | 50 | 120 |

---

### 4.7 سیستم رتبه‌بندی (Leaderboard)

جدول رتبه‌بندی بر اساس **ثروت** (`wealth`) مرتب شده و شامل:

- رتبه (1-8)
- آواتار و نام کاربری
- سطح و تعداد کسب‌وکار
- ثروت (تومان)
- نمایش ویژه برای 3 رتبه اول: طلایی، نقره‌ای، برنزی
- بازیکن فعلی با رنگ ایندیگو هایلایت و برچسب "(شما)"
- نمایش "اقتصاد کل" (مجموع ثروت تمام بازیکنان)

#### داده‌های رتبه‌بندی (Mock)

| رتبه | نام | آواتار | ثروت | سطح | کسب‌وکار |
|------|------|--------|------|------|---------|
| 1 | سلطان‌تجارت | 👑 | 2,500,000 | 18 | 8 |
| 2 | مگاکورپ | 🦈 | 1,800,000 | 15 | 6 |
| 3 | طلاجو | 💰 | 1,200,000 | 12 | 5 |
| 4 | شاه‌مزرعه | 🌾 | 950,000 | 10 | 4 |
| 5 | نانوایی‌باشی | 🍞 | 780,000 | 9 | 3 |
| 6 | تاجرباشی (بازیکن) | 👤 | 150,000 | 5 | 1 |
| 7 | ملکه‌لبنیات | 🥛 | 120,000 | 4 | 2 |
| 8 | جادوگرفناوری | 📱 | 85,000 | 3 | 1 |

---

### 4.8 سیستم بازار جمعه (Friday Market)

آیتم‌های ویژه‌ای که بازیکن می‌تواند برای **تقویت وضعیت شخصی** خریداری کند. در صفحه پروفایل نمایش داده می‌شود.

| آیتم | آیکون | قیمت (تومان) | اثرات | در دسترس |
|------|-------|-------------|-------|----------|
| نوشیدنی انرژی‌زا | ⚡ | 500 | انرژی +20 | بله |
| کتاب آموزشی | 📚 | 2,000 | هوش +10 | بله |
| بشقاب کباب | 🍖 | 300 | گرسنگی -30، شادی +10 | بله |
| قهوه | ☕ | 150 | انرژی +10، شادی +5 | بله |
| کنسول بازی | 🎮 | 5,000 | شادی +25 | خیر |
| اشتراک باشگاه | 💪 | 1,000 | انرژی +15، شادی +10 | بله |

#### مکانیک خرید آیتم بازار جمعه

```
newStats[key] = max(0, min(100, currentStat + effect))
balance -= item.price
```

---

### 4.9 سیستم ماموریت‌های روزانه (Daily Tasks)

در صفحه اصلی نمایش داده می‌شود (فعلا static):

- یک کارمند جدید استخدام کن
- یک کسب‌وکار را ارتقا بده
- محصولی در بازار بفروش (تکمیل‌شده)

---

### 4.10 سیستم ظرفیت کسب‌وکار (Business Capacity System)

هر کسب‌وکار محدودیت ظرفیت دارد که با ارتقا سطح افزایش می‌یابد:

| پارامتر | مقدار اولیه | رشد | توضیح |
|----------|-------------|------|-------|
| `maxEmployees` | 3 | +1 هر 3 سطح ارتقا | حداکثر تعداد نیرو |
| `maxProducts` | 2 | +1 هر 5 سطح ارتقا | حداکثر تعداد محصولات آنلاک‌شده |
| `maxLevel` | 20 | — | سقف سطح کسب‌وکار |

#### محاسبه ظرفیت

```
maxEmployees = 3 + floor((level - 1) / 3)
maxProducts = 2 + floor((level - 1) / 5)
```

#### نمایش در UI

- تعداد نیرو: `"نیرو: 1/3"`
- هنگام پر بودن ظرفیت: `"ظرفیت پر"` روی دکمه‌های استخدام/آنلاک

---

### 4.11 موتور اقتصاد سراسری (Global Economy Engine)

سیستم تغییر قیمت‌های بازار به صورت پویا:

#### مکانیک به‌روزرسانی

تابع `updateMarketPrices()` هر **5 دقیقه** توسط `useGameTick` فراخوانی می‌شود.

#### قوانین تغییر قیمت

| پارامتر | مقدار |
|----------|-------|
| **بازه تغییر قیمت** | ±5% تا ±15% به صورت تصادفی |
| **کف قیمت** | 50% از `basePrice` |
| **سقف قیمت** | 200% از `basePrice` |
| **دریفت عرضه/تقاضا** | ±25 تصادفی |
| **تاریخچه قیمت** | آخرین 7 مقدار نگه‌داری می‌شود |

#### شبه‌کد

```
for each product in marketProducts:
    changePercent = random(-15, +15) / 100  // حداقل ±5%
    newPrice = round(currentPrice * (1 + changePercent))
    newPrice = clamp(newPrice, basePrice * 0.5, basePrice * 2.0)
    currentPrice = newPrice

    supply += random(-25, +25)
    demand += random(-25, +25)

    priceHistory.push(currentPrice)
    if priceHistory.length > 7:
        priceHistory.shift()
```

---

## 5. فرمول‌ها و محاسبات

### 5.1 درآمد موثر (Effective Revenue)

```
staffBoostSum = SUM(emp.revenueBoost) for all employees
staffMultiplier = min(1 + staffBoostSum, 2.5)  // max 150% boost
effectiveRevenue = round(baseRevenue * staffMultiplier) + SUM(unlocked product.revenueBoost)
```

**مثال**: کسب‌وکار با `baseRevenue = 5,000`، یک محصول آنلاک‌شده (`revenueBoost = 3,000`) و یک برنامه‌نویس (`revenueBoost = 0.3`):
```
staffBoostSum = 0.3
staffMultiplier = min(1 + 0.3, 2.5) = 1.3
effectiveRevenue = round(5,000 * 1.3) + 3,000 = 6,500 + 3,000 = 9,500
```

**مثال سقف**: کسب‌وکار با تمام 5 کارمند (مجموع بوست = 1.8):
```
staffBoostSum = 0.30 + 0.50 + 0.00 + 0.20 + 0.15 = 1.15
staffMultiplier = min(1 + 1.15, 2.5) = 2.15  // زیر سقف
```

اگر بوست مجموعا 1.6 یا بیشتر باشد:
```
staffBoostSum = 1.6
staffMultiplier = min(1 + 1.6, 2.5) = 2.5  // سقف اعمال شد
```

### 5.2 هزینه کل (Total Expenses)

```
totalExpenses = baseExpenses + SUM(employee.salary) for all employees
```

### 5.3 سود خالص هر سیکل (Net Profit)

```
netPerCycle = max(0, effectiveRevenue - totalExpenses)
```

### 5.4 مقیاس‌گذاری هزینه ارتقا (Upgrade Cost Scaling)

```
// هزینه اولیه ارتقا (هنگام ساخت):
initialUpgradeCost = round(startCost * 1.5)

// پس از هر ارتقا:
newUpgradeCost = round(currentUpgradeCost * 1.6)
```

**مثال برای استارتاپ** (`startCost = 50,000`):

| سطح | upgradeCost |
|------|------------|
| 1 → 2 | 75,000 |
| 2 → 3 | 120,000 |
| 3 → 4 | 192,000 |
| 4 → 5 | 307,200 |
| 5 → 6 | 491,520 |

### 5.5 مقیاس‌گذاری درآمد پایه (Base Revenue Scaling)

```
newBaseRevenue = round(currentBaseRevenue * 1.25)
```

**مثال برای استارتاپ** (`baseRevenue = 5,000`):

| سطح | baseRevenue |
|------|------------|
| 1 | 5,000 |
| 2 | 6,250 |
| 3 | 7,813 |
| 4 | 9,766 |
| 5 | 12,207 |

### 5.6 درآمد انباشته (Pending Revenue)

```
cappedCycles = min(completedCycles, maxPendingCycles)
newPending = pendingRevenue + netPerCycle * cappedCycles
maxPending = netPerCycle * maxPendingCycles
actualPending = min(newPending, maxPending)
```

### 5.7 ارزش امپراتوری (Empire Value)

```
companyValue = baseRevenue * level * 10
empireValue = balance + SUM(companyValue) for all businesses
```

**مثال**: بازیکن با `balance = 150,000` و یک استارتاپ سطح 3 (`baseRevenue = 7,813`):
```
companyValue = 7,813 * 3 * 10 = 234,390
empireValue = 150,000 + 234,390 = 384,390
```

### 5.8 درصد تغییر قیمت (Price Change)

```
priceChange = ((currentPrice - basePrice) / basePrice) * 100
```

---

## 6. قالب‌های کسب‌وکار

### 6.1 استارتاپ نرم‌افزاری (app_startup) 📱

| ویژگی | مقدار |
|--------|-------|
| **نام پیش‌فرض** | داده‌پردازان |
| **توضیح** | شرکت برنامه‌نویسی. با یک برنامه‌نویس ساده و تجهیزات سخت‌افزاری شروع کنید. |
| **هزینه آغاز** | 50,000 تومان |
| **درآمد پایه** | 5,000 تومان/سیکل |
| **مدت سیکل** | 180 ثانیه (3 دقیقه) |
| **هزینه پایه** | 1,000 تومان/سیکل |
| **حداکثر انباشت** | 50 سیکل |
| **تجهیزات اولیه** | لپتاپ و میز کار |

**کارمندان قابل استخدام:**

| نام | نقش | حقوق/سیکل | بوست درآمد | هزینه استخدام | ویژه |
|------|------|-----------|-----------|--------------|------|
| برنامه‌نویس ارشد 👨‍💻 | base | 2,000 | +30% | 20,000 | — |
| مدیر پروژه 👔 | manager | 3,500 | +50% | 50,000 | — |
| حسابدار شرکت 🧮 | accountant | 2,500 | 0% | 30,000 | جمع‌آوری خودکار |
| بازاریاب دیجیتال 📢 | marketer | 1,800 | +20% | 15,000 | — |
| کارشناس فروش 🤝 | sales | 1,500 | +15% | 10,000 | — |

**محصولات قابل آنلاک:**

| نام | آیکون | هزینه آنلاک | بوست درآمد/سیکل |
|------|-------|------------|----------------|
| اپلیکیشن فروشگاهی | 🛒 | 30,000 | +3,000 |
| سامانه حسابداری | 📊 | 50,000 | +5,000 |
| بازی موبایل | 🎮 | 80,000 | +8,000 |

---

### 6.2 مزرعه (farming) 🌾

| ویژگی | مقدار |
|--------|-------|
| **نام پیش‌فرض** | مزرعه سبز |
| **توضیح** | کشت محصول و پرورش دام. پایه زنجیره تامین. |
| **هزینه آغاز** | 30,000 تومان |
| **درآمد پایه** | 3,000 تومان/سیکل |
| **مدت سیکل** | 120 ثانیه (2 دقیقه) |
| **هزینه پایه** | 800 تومان/سیکل |
| **حداکثر انباشت** | 50 سیکل |
| **تجهیزات اولیه** | زمین کشاورزی و ابزار دستی |

**کارمندان قابل استخدام:**

| نام | نقش | حقوق/سیکل | بوست درآمد | هزینه استخدام | ویژه |
|------|------|-----------|-----------|--------------|------|
| کشاورز ماهر 🧑‍🌾 | base | 1,200 | +25% | 10,000 | — |
| مدیر مزرعه 👔 | manager | 2,800 | +40% | 40,000 | — |
| حسابدار 🧮 | accountant | 2,000 | 0% | 25,000 | جمع‌آوری خودکار |

**محصولات قابل آنلاک:**

| نام | آیکون | هزینه آنلاک | بوست درآمد/سیکل |
|------|-------|------------|----------------|
| گلخانه | 🌿 | 20,000 | +2,000 |
| دامداری | 🐄 | 40,000 | +4,000 |
| زنبورداری | 🍯 | 15,000 | +1,500 |

---

### 6.3 رستوران (restaurant) 🍽️

| ویژگی | مقدار |
|--------|-------|
| **نام پیش‌فرض** | رستوران لذیذ |
| **توضیح** | تبدیل مواد اولیه به غذا. حاشیه سود بالا با مدیریت خوب. |
| **هزینه آغاز** | 80,000 تومان |
| **درآمد پایه** | 8,000 تومان/سیکل |
| **مدت سیکل** | 150 ثانیه (2.5 دقیقه) |
| **هزینه پایه** | 2,000 تومان/سیکل |
| **حداکثر انباشت** | 50 سیکل |
| **تجهیزات اولیه** | آشپزخانه صنعتی و سالن غذاخوری |

**کارمندان قابل استخدام:**

| نام | نقش | حقوق/سیکل | بوست درآمد | هزینه استخدام | ویژه |
|------|------|-----------|-----------|--------------|------|
| سرآشپز 👨‍🍳 | base | 2,000 | +35% | 25,000 | — |
| مدیر رستوران 👔 | manager | 3,000 | +45% | 45,000 | — |
| حسابدار 🧮 | accountant | 2,200 | 0% | 30,000 | جمع‌آوری خودکار |
| گارسون حرفه‌ای 🍽️ | sales | 1,000 | +10% | 8,000 | — |

**محصولات قابل آنلاک:**

| نام | آیکون | هزینه آنلاک | بوست درآمد/سیکل |
|------|-------|------------|----------------|
| منوی ویژه | ⭐ | 25,000 | +2,500 |
| سرویس بیرون‌بر | 🛵 | 35,000 | +3,500 |
| کترینگ | 🎉 | 50,000 | +5,000 |

---

### 6.4 کارخانه (factory) 🏭

| ویژگی | مقدار |
|--------|-------|
| **نام پیش‌فرض** | کارخانه تولیدی |
| **توضیح** | فرآوری مواد خام به کالاهای نهایی با سود بیشتر. |
| **هزینه آغاز** | 120,000 تومان |
| **درآمد پایه** | 10,000 تومان/سیکل |
| **مدت سیکل** | 240 ثانیه (4 دقیقه) |
| **هزینه پایه** | 3,000 تومان/سیکل |
| **حداکثر انباشت** | 50 سیکل |
| **تجهیزات اولیه** | خط تولید اولیه |

> **نکته**: کارمندان و محصولات فعلا از قالب استارتاپ استفاده می‌کنند (placeholder).

---

### 6.5 هایپرمارکت (supermarket) 🏪

| ویژگی | مقدار |
|--------|-------|
| **نام پیش‌فرض** | هایپرمارکت |
| **توضیح** | فروش مستقیم محصولات. حجم بالا، درآمد پایدار. |
| **هزینه آغاز** | 70,000 تومان |
| **درآمد پایه** | 6,000 تومان/سیکل |
| **مدت سیکل** | 90 ثانیه (1.5 دقیقه) |
| **هزینه پایه** | 1,500 تومان/سیکل |
| **حداکثر انباشت** | 50 سیکل |
| **تجهیزات اولیه** | قفسه‌ها و صندوق فروش |

> **نکته**: کارمندان و محصولات فعلا از قالب استارتاپ استفاده می‌کنند (placeholder).

---

### 6.6 حمل‌ونقل (transport) 🚛

| ویژگی | مقدار |
|--------|-------|
| **نام پیش‌فرض** | حمل‌ونقل سریع |
| **توضیح** | جابجایی کالا بین کسب‌وکارها. ضروری برای زنجیره تامین. |
| **هزینه آغاز** | 150,000 تومان |
| **درآمد پایه** | 12,000 تومان/سیکل |
| **مدت سیکل** | 300 ثانیه (5 دقیقه) |
| **هزینه پایه** | 4,000 تومان/سیکل |
| **حداکثر انباشت** | 50 سیکل |
| **تجهیزات اولیه** | یک کامیون و راننده |

> **نکته**: کارمندان و محصولات فعلا از قالب استارتاپ استفاده می‌کنند (placeholder).

---

### جدول مقایسه‌ای قالب‌ها

| نوع | هزینه آغاز | درآمد پایه | سیکل (ثانیه) | هزینه پایه | سود خالص پایه | درآمد/دقیقه | انباشت |
|------|-----------|-----------|-------------|-----------|--------------|------------|--------|
| مزرعه 🌾 | 30K | 3K | 120 | 800 | 2,200 | 1,100 | 50 |
| استارتاپ 📱 | 50K | 5K | 180 | 1K | 4,000 | 1,333 | 50 |
| هایپرمارکت 🏪 | 70K | 6K | 90 | 1.5K | 4,500 | 3,000 | 50 |
| رستوران 🍽️ | 80K | 8K | 150 | 2K | 6,000 | 2,400 | 50 |
| کارخانه 🏭 | 120K | 10K | 240 | 3K | 7,000 | 1,750 | 50 |
| حمل‌ونقل 🚛 | 150K | 12K | 300 | 4K | 8,000 | 1,600 | 50 |

> **K** = هزار تومان. "درآمد/دقیقه" = سود خالص پایه / (سیکل / 60)

---

## 7. صفحات و رابط کاربری

### 7.1 صفحه اصلی (Home — `/`)

داشبورد اصلی امپراتوری بازیکن، شامل:

1. **خوش‌آمدگویی**: نام کاربری + عنوان "داشبورد امپراتوری کسب‌وکار شما"
2. **خلاصه امپراتوری** (گرید 2 ستونه):
   - موجودی کل (با glow ایندیگو)
   - سود/سیکل (با glow سبز)
3. **آمار سریع** (گرید 3 ستونه): ارزش امپراتوری، تعداد کسب‌وکار، تعداد نیروها
4. **کسب‌وکارهای من**: لیست کسب‌وکارها با درآمد/هزینه/سود + لینک "مشاهده همه"
5. **روند بازار**: 4 محصول پرنوسان‌ترین با تغییرات درصدی + عرضه/تقاضا
6. **ماموریت‌های روزانه**: 3 ماموریت با چک‌باکس
7. **وضعیت شخصی**: 5 آمار با اموجی و عدد (لینک به پروفایل)
8. **دکمه شناور**: "مدیریت کسب‌وکارها" (fixed، پایین صفحه، بالای BottomNav)

### 7.2 صفحه کسب‌وکارها (Business List — `/business`)

1. **هدر**: عنوان + دکمه "+ جدید"
2. **خلاصه** (گرید 2 ستونه): درآمد/سیکل، سود/سیکل
3. **آمار متنی**: تعداد نیرو، هزینه/سیکل
4. **لیست کسب‌وکارها**: هر کدام یک `BusinessCard` با تایمر و progress bar
5. **حالت خالی**: پیام تشویقی + دکمه "ساخت کسب‌وکار"
6. **مودال ساخت** (`NewBusinessModal`): انتخاب قالب + نام سفارشی + دکمه ساخت

### 7.3 صفحه جزئیات کسب‌وکار (Business Detail — `/business/[id]`)

1. **هدر**: آیکون + نام + سطح + تجهیزات اولیه + دکمه بازگشت
2. **تایمر دایره‌ای** (SVG progress ring): نمایش زمان باقیمانده + دکمه جمع‌آوری
3. **آمار مالی** (گرید 3 ستونه): درآمد/هزینه/سود خالص
4. **سه تب**:
   - **کلی**: اطلاعات شرکت + دکمه ارتقا با قیمت
   - **نیروها**: لیست نیروهای فعلی + لیست نیروهای قابل استخدام + نمایش ظرفیت (مثلا "نیرو: 1/3")
   - **محصولات**: لیست محصولات قفل/آنلاک + دکمه آنلاک + نمایش ظرفیت

### 7.4 صفحه بازار (Market — `/market`)

1. **تب قیمت‌ها**: لیست تمام محصولات بازار با `ProductPriceCard` (نمودار مینی + قیمت + عرضه/تقاضا)
2. **تب آگهی‌ها**: لیست آگهی‌های فروش بازیکنان با `MarketListingCard` (خرید 1 واحد)

### 7.5 صفحه رتبه‌بندی (Leaderboard — `/leaderboard`)

1. **کارت اقتصاد کل**: مجموع ثروت + تعداد بازیکنان فعال
2. **لیست رتبه‌بندی**: هر ردیف با رنگ‌بندی ویژه (طلا/نقره/برنز) + هایلایت بازیکن فعلی

### 7.6 صفحه پروفایل (Profile — `/profile`)

1. **هدر پروفایل**: آواتار + نام + سطح + اعتبار + موجودی
2. **اطلاعات سریع** (گرید 3 ستونه): سطح، تعداد کسب‌وکار، سال عضویت
3. **وضعیت شخصی**: 5 نوار پیشرفت (`StatBar`) با رنگ‌های مختلف
4. **بازار جمعه** (گرید 2 ستونه): آیتم‌های قابل خرید با اثرات و قیمت

### 7.7 صفحه تنظیمات (Settings — `/settings`)

1. **واحد پولی**: 5 گزینه (تومان، ریال، دلار، یورو، سکه) — ذخیره در Zustand
2. **صدای بازی**: Toggle switch (فعلا بدون عملکرد)
3. **اعلان‌ها**: Toggle switch (فعلا بدون عملکرد)
4. **زبان**: فقط فارسی (غیرقابل تغییر)
5. **درباره بازی**: نام + نسخه

---

## 8. سیستم ناوبری

### 8.1 نوار بالا (TopBar)

- **موقعیت**: `fixed top-0` با `z-50`
- **پس‌زمینه**: `bg-zinc-900/95 backdrop-blur-md`
- **محتوا** (چپ به راست در RTL):
  - آواتار + نام کاربری + سطح
  - نشانگر انرژی (آیکون Zap + مقدار)
  - موجودی (MoneyDisplay)
  - لینک تنظیمات (آیکون Settings)

### 8.2 نوار پایین (BottomNav)

- **موقعیت**: `fixed bottom-0` با `z-50`
- **پس‌زمینه**: `bg-zinc-900/95 backdrop-blur-md`
- **5 آیتم**:

| برچسب | مسیر | آیکون |
|--------|------|-------|
| خانه | `/` | Home |
| کسب‌وکار | `/business` | Briefcase |
| بازار | `/market` | ShoppingCart |
| رتبه‌بندی | `/leaderboard` | Trophy |
| پروفایل | `/profile` | User |

- تب فعال: رنگ `text-indigo-400` + `strokeWidth: 2.5` + نشانگر خط پایین (`h-0.5 bg-indigo-400`)

### 8.3 Shell بازی (GameShell)

تمام صفحات درون `GameShell` قرار می‌گیرند. هوک `useGameTick` در همین کامپوننت اجرا می‌شود و تایمر سراسری بازی را مدیریت می‌کند:

```
<TopBar />
<main className="max-w-lg mx-auto pt-16 pb-20 px-4">
  {children}
</main>
<BottomNav />
```

- `pt-16`: فاصله برای TopBar (64px)
- `pb-20`: فاصله برای BottomNav (80px)
- `max-w-lg`: حداکثر عرض 512px (Mobile-first)
- `useGameTick`: تایمر سراسری (هر 1 ثانیه tick کسب‌وکارها + هر 5 دقیقه به‌روزرسانی قیمت بازار)

---

## 9. طراحی بصری

### 9.1 تم رنگی (Dark Theme)

| عنصر | رنگ | کد |
|------|-----|-----|
| پس‌زمینه اصلی | zinc-950 | `#09090b` |
| متن اصلی | سفید | `#fafafa` |
| کارت‌ها | zinc-800/60 | `rgba(39,39,42,0.6)` |
| حاشیه کارت | zinc-700/50 | شفافیت 50% |
| متن ثانویه | zinc-500 | `#71717a` |
| متن ضعیف | zinc-600 | `#52525b` |
| رنگ اصلی (Primary) | indigo-600 | `#4f46e5` |
| رنگ موفقیت | emerald-600/400 | `#059669` / `#34d399` |
| رنگ خطر | red-600/400 | `#dc2626` / `#f87171` |
| رنگ هشدار | amber-400 | `#fbbf24` |
| رنگ پول | amber-400 | `#fbbf24` |
| رنگ نوار بالا/پایین | zinc-900/95 | شفافیت 95% + backdrop-blur |

### 9.2 فونت

- **فونت اصلی**: `Vazirmatn` (نسخه 33.0.3) — فونت فارسی مناسب وب
- **فونت ثانویه**: `system-ui, sans-serif`
- **فونت مونو**: `Geist Mono` (برای اعداد لاتین)
- **کلاس ویژه**: `.font-fa` برای نمایش اعداد فارسی با `font-variant-numeric: tabular-nums`

### 9.3 جهت (Direction)

```html
<html lang="fa" dir="rtl">
```

تمام layout بازی **راست به چپ** (RTL) است. Tailwind CSS به صورت خودکار RTL را مدیریت می‌کند.

### 9.4 Viewport

```typescript
export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,       // جلوگیری از زوم (حس بازی موبایلی)
  themeColor: "#09090b",
};
```

### 9.5 حس بازی موبایلی (Mobile Game Feel)

- **غیرفعال‌سازی انتخاب متن**: `user-select: none` (به‌جز input/textarea)
- **غیرفعال‌سازی tap highlight**: `-webkit-tap-highlight-color: transparent`
- **غیرفعال‌سازی overscroll**: `overscroll-behavior: none`
- **انیمیشن فشردن**: `active:scale-95` روی دکمه‌ها
- **hover scale**: `hover:scale-[1.02]` روی کارت‌های کلیک‌پذیر

### 9.6 اسکرول‌بار

```css
::-webkit-scrollbar { width: 4px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: #3f3f46; border-radius: 999px; }
```

### 9.7 انیمیشن Glow

```css
@keyframes pulse-glow {
  0%, 100% { opacity: 0.6; }
  50% { opacity: 1; }
}
```

کامپوننت `Card` از پراپ `glow` برای ایجاد `box-shadow` با شفافیت `22` (hex) استفاده می‌کند:
```typescript
style={{ boxShadow: `0 0 20px ${glow}22` }}
```

### 9.8 کامپوننت‌های UI

#### Card
- `rounded-2xl`, `bg-zinc-800/60`, `border border-zinc-700/50`, `backdrop-blur-sm`
- Glow اختیاری (سبز هنگام داشتن درآمد آماده)

#### Button
- 4 واریانت: `primary` (ایندیگو), `secondary` (zinc), `danger` (قرمز), `success` (سبز)
- 3 سایز: `sm`, `md`, `lg`
- `rounded-xl`, `active:scale-95`, `disabled:opacity-40`

#### MoneyDisplay
- فرمت اعداد فارسی: `Intl.NumberFormat('fa-IR')`
- واحد پولی پویا از Zustand store
- رنگ: زرد (پیش‌فرض), سبز (مثبت + showSign), قرمز (منفی + showSign)

#### Badge
- `rounded-full`, رنگ پس‌زمینه پویا
- پیش‌فرض: ایندیگو

#### StatBar
- نوار پیشرفت افقی با `max=100`
- رنگ و آیکون سفارشی

---

## 10. برنامه‌های آینده

بر اساس تحلیل کد، موارد زیر قابلیت توسعه دارند:

### 10.1 سیستم‌های ناتمام (در کد موجود اما غیرفعال)

1. **زنجیره تولید (ProductionChain)**: اینترفیس `ProductionChain` در types تعریف شده اما هنوز پیاده‌سازی نشده. این سیستم امکان تبدیل مواد خام به محصولات فرآوری‌شده را فراهم می‌کند:
   ```typescript
   interface ProductionChain {
     input: { productId: string; quantity: number }[];
     output: { productId: string; quantity: number };
     businessType: BusinessType;
     duration: number;
   }
   ```

2. **اقلام خانه (HomeItem)**: اینترفیس `HomeItem` تعریف شده اما استفاده نمی‌شود — احتمالا برای سیستم خانه/مسکن بازیکن.

3. **Toggle Switch های تنظیمات**: صدا و اعلان‌ها فقط UI دارند، بدون عملکرد واقعی.

4. **سیستم فروش**: بازیکن فعلا نمی‌تواند آگهی فروش ایجاد کند (فقط خرید).

### 10.2 بهبودهای پیشنهادی

5. **کارمندان و محصولات اختصاصی**: قالب‌های کارخانه، هایپرمارکت و حمل‌ونقل فعلا از کارمندان/محصولات استارتاپ استفاده می‌کنند (placeholder). باید مجموعه‌های اختصاصی تعریف شوند.

6. **سیستم سطح‌بندی بازیکن**: فعلا سطح و تجربه بازیکن ثابت است و مکانیک level-up وجود ندارد.

7. **ماموریت‌های روزانه پویا**: فعلا hardcoded هستند — نیاز به سیستم تشخیص خودکار انجام ماموریت‌ها.

8. **Persistence**: داده‌ها فقط در حافظه (Zustand) نگه‌داری می‌شوند و با refresh از بین می‌روند. نیاز به `localStorage` یا backend.

9. **Multiplayer واقعی**: فعلا تمام بازیکنان mock هستند. نیاز به سرور و احراز هویت.

10. **سیستم اعلان‌ها**: اطلاع‌رسانی هنگام آماده شدن درآمد، اتمام سیکل، و...

11. **تاثیر وضعیت شخصی بر کسب‌وکار**: فعلا آمار شخصی (شادی، انرژی و...) تاثیری بر عملکرد کسب‌وکارها ندارد.

---

> **نسخه سند**: 2.0
> **تاریخ تنظیم**: 1404/12/20
> **بر اساس کد نسخه**: 0.2.0
